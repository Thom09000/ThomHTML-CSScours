
// alert("Bonjour");

//MODEL EXERCICE

//Ax

// Initialisation



// Execution

//A1
/*
// Initialisation turtle 
setPos(300, 300);
changeColor(color.red);
setLineWidth(10);
faceDown();

// Execution 
forward(200);
faceRight();
forward();
*/

/*
//A2

// Initialisation

setPos(300, 300);
changeColor(color.yellow);
setLineWidth(10);
faceLeft();

// Execution

forward(100)
faceDown()
forward(100)
faceRight()
forward(200)
faceUp()
forward(200)
*/

//A3

/*
// Initialisation

setPos(300, 300);
changeColor(color.yellow);
setLineWidth(10);
faceUp();

// Initialisation

forward(100)
backward(100)
faceLeft(100)
forward(100)
backward(100)

faceDown()
forward(100)
backward(100)
faceRight(100)
forward(100)
backward(100)
*/

/*
//A4

// Initialisation

setPos(200, 400);
changeColor(color.green);
setLineWidth(10);
faceRight();

// Execution

forward(100);
arcLeft(50,180);
forward(50)
arcRight(50,180);
forward(100);
*/

//A5
/*
// Initialisation

setPos(300, 150);
changeColor(color.fuchsia);
setLineWidth(10);
faceRight();

// Execution

arcRight(150,180);
arcRight(150,180);
arcRight(75,180);
arcLeft(75,180);
*/


/*
//A6

// Initialisation

setPos(300, 500);
changeColor(color.fuchsia);
setLineWidth(10);
faceRight();

// Execution

faceUp();
forward(100);
up();
forward(100);
down();
forward(200)
*/


/*
//A7

// Initialisation

setPos(100, 300);
changeColor(color.green);
setLineWidth(10);
faceDown();

// Execution

forward(100);
arcLeft(50, 180)
forward(50)
up();
forward(50)
down();
forward(100);
arcLeft(50, 180)
forward(50)
*/


/*
//A8

// Initialisation

setPos(250, 300);
changeColor(color.fuchsia);
setLineWidth(10);
faceUp();

// Execution

forward(50);
faceRight()
forward(100);
faceDown()
forward(200);
up();
faceLeft()
forward(100)
faceUp()
forward(50)
down()
forward(50)
*/

/*
//A8

// Initialisation

setPos(150, 400);
changeColor(color.blue);
setLineWidth(10);
faceRight();

// Execution

forward(300)
faceLeft()
arcRight(50, 180)
arcLeft(50, 180)
forward(300)

arcLeft(50, 180)
arcRight(50, 180)
*/

/*
//A8

// Initialisation

setPos(300, 400);
changeColor(color.red);
setLineWidth(10);
faceRight();

// Execution

arcLeft(50, 180);
changeColor(color.green);
arcLeft(100, 180);
changeColor(color.yellow);
arcLeft(200, 180);
*/



/*
//B1

// Initialisation

setPos(300, 300);
changeColor(color.green);
setLineWidth(10);
faceRight();

// Execution

for(let i = 0 ; i < 4 ; i++) {

    forward(50);
    faceDown(50);
    forward(50);
    faceRight(20);
}
*/

/*
//B2

// Initialisation

setPos(100, 300);
changeColor(color.red);
setLineWidth(10);
faceUp();

// Execution

for(let i = 0 ; i < 4 ; i++) {

    forward(50);
    faceRight();
    forward(50)
    faceDown();
    forward(50)
    faceRight()
    forward(50)
    faceUp()    
}
    */

/*
//B3

// Initialisation

setPos(300, 300);
changeColor(color.fuchsia);
setLineWidth(10);
faceRight();

// Execution

for(let i = 0 ; i < 4 ; i++) {
    forward(100);
    right(90);
}
*/

/*
//B4

// Initialisation

setPos(300, 300);
changeColor(color.aqua);
setLineWidth(10);
faceRight();

// Execution

for(let i = 0 ; i < 3 ; i++) {
    forward(100);
    left(180-60);
}
*/

/*
//B5

// Initialisation

setPos(300, 450);
changeColor(color.black);
setLineWidth(10);
faceUp();

// Execution

forward(200)
changeColor(color.red)
for(let i = 0 ; i < 3 ; i++) {
    forward(100);
    right(180-60);
}
    */

/*
//B6

// Initialisation

setPos(300, 300);
changeColor(color.black);
setLineWidth(10);
faceRight();

// Execution

changeColor(color.red)
for(let i = 0 ; i < 2 ; i++) {
    forward(50);
    faceUp();
    forward(50)
    faceRight()
}
changeColor(color.green)
left(180-180);
for(let j = 0 ; j < 3 ; j++) {
    forward(50);
    faceDown();
    forward(50)
    faceRight()
}
*/

/*
//B7

// Initialisation

setPos(300, 300);
changeColor(color.black);
setLineWidth(10);
faceRight();

// Execution

for(let i = 0 ; i < 4 ; i++) {
    forward(100);
    right(90)
}
for(let i = 0 ; i < 4 ; i++) {
    forward(100);
    left(180-60)
}
*/

/*
//B8

// Initialisation

setPos(200, 200);
changeColor(color.black);
setLineWidth(10);
faceRight();

// Execution

for(let i = 0 ; i < 9 ; i++) {
    forward(300);
    right(180-45);
}
*/


/*
//B9

// Initialisation

setPos(200, 200);
changeColor(color.fuchsia);
setLineWidth(10);
faceRight();

// Execution

forward(100);
up();
forward(50)
down();
for(let i = 0 ; i < 4 ; i++) {
    forward(100);
    right(180-60);
}
*/

/*
//B10

// Initialisation

setPos(100, 350);
changeColor(color.aqua);
setLineWidth(10);
faceRight();

// Execution

for(let i = 0 ; i < 8 ; i++) {
    forward(100);
    right(180-45);
}
up();
forward(250)
down();
changeColor(color.yellow)
for(let i = 0 ; i < 10 ; i++) {
    forward(200);
    left(180-45);
}
up();
forward(250)
down();
changeColor(color.fuchsia)
for(let i = 0 ; i < 10 ; i++) {
    forward(100);
    right(180-45);
}
*/

/*
//B11

// Initialisation

setPos(250, 150);
changeColor(color.red);
setLineWidth(10);
faceRight();

// Execution

for(let i = 0 ; i < 4 ; i++) {
forward(100);
right(90);
forward(100);
left(90);
forward(100)
right(180-90)
}
*/

/*
//B12

// Initialisation

setPos(300, 300);
changeColor(color.fuchsia);
setLineWidth(10);
faceRight();

// Execution


faceDown();

verre();

up();
forward(50)
down();

changeColor(color.green);
left(180-180);
verre()
*/

/*
//B13

// Initialisation

setPos(300, 300);
changeColor(color.green);
setLineWidth(10);
faceRight();

// Execution

function shape() {
    forward(50);
    left(180-45);
    forward(25);
    right(90);
    forward(25);
    right(90);
    
}

for(let i = 0 ; i < 10 ; i++) {
shape()
}
*/

/*
//B14

// Initialisation

setPos(100, 100);
changeColor(color.green);
setLineWidth(10);
faceRight();

// Execution

for(let i = 0 ; i < 8 ; i++) {
    forward(50);
    right(180-60);
    forward(100);
    left(180-60)
    forward(50)
}
    */

/*
//B15

// Initialisation

setPos(300, 300);
changeColor(color.red);
setLineWidth(10);
faceRight();

// Execution

for(let i = 0 ; i < 8 ; i++) {
forward(100)
arcLeft(50,90)
}
*/

/*
//B16

// Initialisation

setPos(300, 300);
changeColor(color.green);
setLineWidth(10);
faceRight();

// Execution

arcLeft(200,90)
left(180-90)
for(let i = 0 ; i < 4 ; i++) {
arcLeft(25,90)
arcRight(25,90)
}
*/

/*
//B17

// Initialisation

setPos(300, 300);
changeColor(color.blue);
setLineWidth(10);
faceRight();

// Execution

for(let i = 0 ; i < 6 ; i++) {
left(180-120)
arcRight(50,180)
left(180-0)
left(180-180)
}
*/

/*
//B18

// Initialisation

setPos(300, 300);
changeColor(color.yellow);
shiftColor(0.2);
setLineWidth(10);
faceRight();

// Execution

function L() {
    forward(200)
    left(180-90)
    forward(50)
    left(180-135)
  
}

for(let i = 0 ; i < 8 ; i++) {
L()
}
*/


/*
//B19

// Initialisation

setPos(300, 550);
changeColor(color.aqua);
setLineWidth(10);
faceRight();

// Execution

function Ci() {
    arcLeft(50,180);
    left(180-0);
  
}

for(let i = 0 ; i < 4 ; i++) {
    Ci();
}

up();
faceUp();
forward(50);
faceLeft();
left(180-180);
down();


for(let i = 0 ; i < 4 ; i++) {
    arcLeft(50,180);
    left(180-0);
}
*/

/*
//B20

// Initialisation

setPos(300, 300);
changeColor(color.aqua);
setLineWidth(10)
faceDown();

// Execution

pas la bonne hauteur en haut mais je vois le principe...

for(let i = 0 ; i < 4 ; i++) {
    forward(100);
    arcLeft(50,90);
}
faceRight();
forward(200);
up();
faceDown();
forward(125);
faceLeft();
forward(100);
down();
arcRight(50,360);
*/

/*
//B21

// Initialisation

setPos(400, 100);
shiftColor(0.6);
setLineWidth(10)
faceRight();

// Execution

function fish() {
    forward(100);
    left(180-60);
    forward(50);
    left(180-60)
    forward(100);
    right(60);
    forward(50);
    right(180-60);
    forward(50);
}

for(let i = 0 ; i < 6 ; i++) {
    fish ()
    up();  
    right(180-60)
    forward(100);
    down();
    shiftColor(0.1);
}
*/


/*
//C1

// Initialisation

setPos(100, 300);
shiftColor(0.6);
setLineWidth(10)
faceRight();

// Execution

forward(400);
faceLeft();
for(let i = 0 ; i < 4 ; i++) {
    left(180-120);
    forward(100);
    left(180-300);
    forward(100);
    faceLeft();
}
    */

/*
//C2

// Initialisation

setPos(300, 550);
shiftColor(0.8);
setLineWidth(10)
faceUp();

// Execution

function square() {
for(let i = 0 ; i < 4 ; i++) {
forward(50);
right(90);
}
}
for(let i = 0 ; i < 5 ; i++) {
square();
up();
forward(100);
down();
}
*/

/*
//C3

// Initialisation

setPos(50, 350);
shiftColor(0.44);
setLineWidth(10)
faceRight();

// Execution

function sun() {
    for(let i = 0 ; i < 8 ; i++) {
    forward(100)
    left(180-45)
}
}

for(let i = 0 ; i < 3 ; i++) {
    sun();
    up();
    forward(200);
    shiftColor(0.1);
    down();
}
*/

/*
//C3

// Initialisation

setPos(50, 350);
shiftColor(0.0);
setLineWidth(10)
faceRight();

// Execution

function house() {
    for(let i = 0 ; i < 3 ; i++) {
    changeColor(color.red);
    forward(50)
    left(180-60)
}
faceDown
for(let i = 0 ; i < 4 ; i++) {
    changeColor(color.white);
    forward(50)
    right(180-90)
}
}

 for(let i = 0 ; i < 5 ; i++) {
    house()
    up();
    forward(100);
    down();
 }
*/

/*
//C3

// Initialisation

setPos(300, 550);
changeColor(color.green);
setLineWidth(10)
faceUp();

// Execution

function flower() {
    for(let i = 0 ; i < 4 ; i++) {
        right(180-135);
        forward(100);
        for(let j = 0 ; j < 3 ; j++) {
            right(180-60);
            changeColor(color.red)
            forward(100);
            }
    changeColor(color.yellow) ;      
    left(180-45)
    
 } 
}


forward(200);

flower()
*/

/*
//D9

// Initialisation

setPos(300, 300);
changeColor(color.blue);
setLineWidth(10)
faceRight();

// Execution

let longueur = 50

for(let i = 0 ; i < 20 ; i++) {
    forward(longueur)
    right(180-60)
    longueur = longueur+50
}

setPos(300, 325);
setLineWidth(10)
faceRight();
changeColor(color.red);
longueur = 50;
for(let i = 0 ; i < 40 ; i++) {
    forward(longueur)
    right(180-60)
    longueur = longueur+45
}
*/

/*
//D10

// Initialisation

setPos(300, 300);
changeColor(color.yellow);
setLineWidth(10)
faceRight();

// Execution

let longueur1 = 50

for(let i = 0 ; i < 10 ; i++) {
forward(longueur1)

right(180-90)
longueur1 = longueur1+25
}
*/


