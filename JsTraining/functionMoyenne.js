/*
let v1 = Math.round(Math.random()*21);
let v2 = Math.round(Math.random()*21);

console.log("valeur random 1",v1);
console.log("valeur random 2",v2);

function average (n,m) {
    let result;
    result = (n+m)/2
    return result;
}
*/

/*
console.log("moyenne valeur random", "La somme des valeurs random "+v1+" et "+v2+" est égale à "+(v1+v2)+" si on la divise par le nombre de valeurs, ontombe donc sur ", average(v1,v2));*/
/*
let notes =[ Math.round(Math.random()*21), Math.round(Math.random()*21), Math.round(Math.random()*21)];
console.log("ensemble des notes élève",notes);
*/
//-----------------------------------------------------

/*
function moystudent(tab) {
    let n=tab[0];
    for (let i=0; i<=tab.length; i++) {
        n=n+tab[i+1];
        console.log("addition de toutes les notes élève",n)
    }
    n/tab.length
    return n;
}
console.log("moyenne de l'élève", moystudent(notes));
*/

//------------------------------------------------------

/*
let ajouterUn = tab.map(function (nombre) { 

    return nombre +  
    }
);
*/

//---------------------------------------------------------

/*
function bilan (tab) {
    if (moystudent(tab)<10) {
        return console.log("Refus. Tu fais partie de la déacadance occidentale, bravo !")
    }
    else if (moystudent(tab)>=10 && moystudent(tab)<15) {
       return console.log("Assez Bien")
    }  
    return console.log("Très Bien");
}


console.log("mention obtenue par l'élève suivant sa moyenne", bilan(notes));
*/

/*
let ajouterUn = tab.map(function (nombre) { 
    tab
    return nombre + 1 
    }
);
*/
